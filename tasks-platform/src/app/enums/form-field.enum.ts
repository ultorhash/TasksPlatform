export enum FormFieldTypes {
  TEXT = 'Input',
  NUMBER = 'Number'
}

export enum FormFieldErrors {
  REQUIRED = 'Field is required',
  ADDRESS = 'Provided address is invalid',
  AMOUNT = `Amount must be equal or greater than 0.005`
}
